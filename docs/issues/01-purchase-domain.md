# Purchase `tarekrecolons.com` domain

Custom domain for the Cloudflare Pages deployment. Currently the site lives at `tarekrecolons.pages.dev`.

**Recommendation:** Buy via Cloudflare Registrar (at-cost, no markup, free WHOIS privacy, automatic DNS integration with Pages).

**Decision needed:**
- Confirm `tarekrecolons.com` is the right domain (alternatives: `tarekrecolons.es`, `tarekrecolons.studio`, `tarekrecolons.cat`)
- Authorize the purchase (~$10–15/year)

**After purchase:**
1. Add custom domain in Cloudflare Pages dashboard → project → Custom domains
2. Cloudflare auto-provisions the SSL cert
3. Set up redirect from `tarekrecolons.pages.dev` → `tarekrecolons.com`

**Status:** Pending user authorization.

**Carried over from:** 2026-07-03 session (R2 setup paused at this step)